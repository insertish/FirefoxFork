// Use private sync server
lockPref("identity.sync.tokenserver.uri", "https://syncserver.insrt.uk/token/1.0/sync/1.5");

// Sync UI configuration (`browser.uiCustomization.state`)
lockPref("services.sync.prefs.sync.browser.uiCustomization.state", true);
